# Copie este arquivo para .env.capacitor e ajuste os valores
# cp .env.capacitor.example .env.capacitor

# URL direta do backend — sem proxy nginx (Capacitor não tem nginx)
VITE_API_URL=https://api.seudominio.com

# Modo obrigatoriamente backend no app nativo
VITE_APP_MODE=backend
